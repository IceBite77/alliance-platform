import type {UiV2Context,UiV2Env} from "./context";
import {esc,renderUiV2FrontShell,uiV2Html} from "./shell";

type CountRow={total:number};
type AwayRow={display_name:string};
type SettingRow={key:string;value:string};
type WeeklyEventRow={event_name:string;event_time:string|null;note:string|null};
type PowerSummaryRow={alliance_power:number|null;power_players:number};
type RankingRow={display_name:string;power:number};
type BirthdayRow={display_name:string;birthday_month:number;birthday_day:number};
type TrainRow={driver_name:string;vip_name:string|null};
type VsSummaryRow={competition_date:string;challenge_name:string;daily_target:number;total:number;participants:number;hit_target:number};
type DsSummaryRow={battle_date:string;battle_time:string|null;team_a_opponent:string;team_b_opponent:string;team_a_score:number|null;team_a_enemy_score:number|null;team_b_score:number|null;team_b_enemy_score:number|null;results_finalised_at:string|null;a_selected:number;a_subs:number;a_played:number;b_selected:number;b_subs:number;b_played:number};
type ShieldSummaryRow={war_date:string;total:number;affected:number};
type ShieldPlayerRow={display_name:string;drop_count:number};
type DemoRosterRow={id:number;display_name:string;rank:number;base_level:number|null;player_power:number|null;squad_1_power:number|null;total_hero_power:number|null;previous_power:number|null};
type AllianceChangeRow={display_name:string;change_type:string;detail:string;change_date:string};

const WEEKDAYS=["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"];

const londonToday=()=>{
  const parts=new Intl.DateTimeFormat("en-GB",{timeZone:"Europe/London",year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(new Date());
  const part=(type:string)=>parts.find(item=>item.type===type)?.value||"";
  return `${part("year")}-${part("month")}-${part("day")}`;
};

const londonWeekday=()=>{
  const day=new Intl.DateTimeFormat("en-GB",{timeZone:"Europe/London",weekday:"long"}).format(new Date());
  return Math.max(1,WEEKDAYS.indexOf(day)+1);
};

const formatPower=(value:number|null|undefined)=>{
  if(value===null||value===undefined||!Number.isFinite(value))return "—";
  if(value>=1_000_000_000)return `${(value/1_000_000_000).toFixed(2).replace(/\.00$/,"")}B`;
  if(value>=1_000_000)return `${(value/1_000_000).toFixed(1).replace(/\.0$/,"")}M`;
  if(value>=1_000)return `${(value/1_000).toFixed(1).replace(/\.0$/,"")}K`;
  return Math.round(value).toLocaleString("en-GB");
};

const rankingCard=(type:string,title:string,rows:RankingRow[],fullCount:number)=>`<article class="dashboard-card"><div class="card-top"><div><span class="card-type">${esc(type)}</span><h3>${esc(title)}</h3></div>${rows.length?`<span class="sample-badge">Top ${Math.min(5,rows.length)} shown</span>`:""}</div>${rows.length?`<div class="leader-list">${rows.slice(0,5).map((row,index)=>`<div class="leader-row"><span class="leader-rank">${index+1}</span><span class="leader-name">${esc(row.display_name)}</span><span class="leader-value">${formatPower(row.power)}</span></div>`).join("")}</div>${rows.length>5?`<div class="leader-more">${Math.min(fullCount,rows.length)-5} more players in the full ranking</div>`:""}`:'<div class="card-empty"><strong>No power data yet</strong><span>Upload the roster to populate this ranking.</span></div>'}</article>`;

const dashboardCss=`<style>
.command-hero{position:relative;overflow:hidden;display:grid;grid-template-columns:minmax(0,1fr) minmax(300px,440px);align-items:center;gap:42px;min-height:390px;margin-bottom:22px;padding:46px 56px;border:1px solid color-mix(in srgb,var(--ui-accent) 58%,var(--ui-line));border-radius:24px;background:radial-gradient(circle at 82% 42%,color-mix(in srgb,var(--ui-accent) 34%,transparent),transparent 34%),radial-gradient(circle at 12% 110%,color-mix(in srgb,var(--ui-user-rank) 12%,transparent),transparent 32%),linear-gradient(118deg,#04111e 0%,#092944 55%,#061522 100%);box-shadow:0 32px 82px rgba(0,0,0,.46),0 0 46px color-mix(in srgb,var(--ui-accent) 9%,transparent),inset 0 1px rgba(255,255,255,.055)}.command-hero::before{position:absolute;inset:0;background:linear-gradient(0deg,color-mix(in srgb,var(--ui-accent) 12%,transparent),transparent 48%),repeating-linear-gradient(90deg,transparent 0,transparent 59px,rgba(255,255,255,.012) 60px);pointer-events:none;content:""}.command-hero::after{position:absolute;left:56px;bottom:0;width:250px;height:4px;background:linear-gradient(90deg,var(--ui-user-rank),var(--ui-accent),transparent);box-shadow:0 0 22px color-mix(in srgb,var(--ui-accent) 78%,transparent);content:""}.command-copy{position:relative;z-index:1;min-width:0}.command-kicker{margin-bottom:12px;color:#7bd4ff;font-size:.75rem;font-weight:950;letter-spacing:.2em;text-transform:uppercase}.command-title{margin:0;max-width:860px;font-size:clamp(3.3rem,6.4vw,5.75rem);font-weight:1000;letter-spacing:-.052em;line-height:.88;text-transform:uppercase;text-shadow:0 8px 30px rgba(0,0,0,.52)}.command-title .gold{display:block;color:var(--ui-user-rank);text-shadow:0 4px 0 rgba(109,69,0,.4),0 10px 30px color-mix(in srgb,var(--ui-user-rank) 24%,transparent)}.command-sub{max-width:760px;margin:20px 0 0;color:#d3e1ec;font-size:1rem;font-weight:720;line-height:1.55}.command-meta{display:flex;align-items:center;gap:9px;flex-wrap:wrap;margin-top:20px}.command-pill{display:inline-flex;align-items:center;padding:8px 12px;border:1px solid rgba(111,183,225,.38);border-radius:999px;background:rgba(5,19,33,.76);color:#c4deed;font-size:.7rem;font-weight:880}.command-pill.live::before{width:8px;height:8px;margin-right:8px;border-radius:50%;background:var(--ui-success);box-shadow:0 0 12px color-mix(in srgb,var(--ui-success) 78%,transparent);content:""}.command-art{position:relative;z-index:1;display:grid;min-height:310px;place-items:center}.command-art::before{position:absolute;width:82%;aspect-ratio:1;border-radius:50%;background:radial-gradient(circle,color-mix(in srgb,var(--ui-accent) 22%,transparent),transparent 68%);filter:blur(5px);content:""}.command-art img{position:relative;display:block;width:min(100%,390px);max-height:345px;object-fit:contain;filter:drop-shadow(0 24px 32px rgba(0,0,0,.65)) drop-shadow(0 0 24px color-mix(in srgb,var(--ui-accent) 20%,transparent))}.command-art-fallback{position:relative;display:grid;width:245px;height:245px;place-items:center;border:1px solid color-mix(in srgb,var(--ui-accent) 55%,var(--ui-line));border-radius:50%;background:radial-gradient(circle,color-mix(in srgb,var(--ui-accent) 22%,var(--ui-surface-3)),var(--ui-surface-3) 68%);color:var(--ui-accent);font-size:2rem;font-weight:1000;letter-spacing:.04em;box-shadow:0 0 48px color-mix(in srgb,var(--ui-accent) 21%,transparent)}
.preview-note{display:flex;align-items:center;justify-content:space-between;gap:14px;margin-bottom:18px;padding:11px 14px;border:1px solid color-mix(in srgb,var(--ui-accent) 35%,var(--ui-line));border-radius:11px;background:color-mix(in srgb,var(--ui-accent) 7%,var(--ui-surface-2));color:var(--ui-soft);font-size:.76rem;line-height:1.45}.preview-note strong{color:var(--ui-accent)}.sample-badge{flex:0 0 auto;padding:5px 8px;border:1px solid var(--ui-line-strong);border-radius:999px;color:var(--ui-secondary);font-size:.6rem;font-weight:900;letter-spacing:.06em;text-transform:uppercase}
.snapshot-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:11px;margin-bottom:22px}.snapshot-card{position:relative;overflow:hidden;padding:17px 16px;border:1px solid color-mix(in srgb,var(--ui-accent) 25%,var(--ui-line));border-radius:13px;background:linear-gradient(180deg,color-mix(in srgb,var(--ui-accent) 6%,var(--ui-surface-2)),var(--ui-surface-2));box-shadow:0 12px 28px rgba(0,0,0,.17)}.snapshot-card::before{position:absolute;top:0;right:0;left:0;height:2px;background:linear-gradient(90deg,var(--ui-accent),transparent 82%);content:""}.snapshot-card:nth-child(2)::before,.snapshot-card:nth-child(4)::before{background:linear-gradient(90deg,var(--ui-user-rank),var(--ui-accent),transparent 88%)}.snapshot-card span,.snapshot-card strong,.snapshot-card small{display:block}.snapshot-card span{color:#9fbed3;font-size:.65rem;font-weight:900;letter-spacing:.07em;text-transform:uppercase}.snapshot-card strong{margin-top:7px;font-size:1.5rem;line-height:1;text-shadow:0 3px 16px rgba(0,0,0,.32)}.snapshot-card small{margin-top:7px;color:var(--ui-secondary);font-size:.67rem}.snapshot-card .positive{color:var(--ui-success)}
.snapshot-card .train-vip{font-size:1.18rem;font-weight:800;line-height:1.05}
.dashboard-section{margin-top:23px}.section-head{display:flex;align-items:end;justify-content:space-between;gap:18px;margin-bottom:11px}.section-head h2{margin:0;font-size:1.12rem}.section-head p{margin:4px 0 0;color:var(--ui-muted);font-size:.78rem}.section-link{color:var(--ui-navigation);font-size:.7rem;font-weight:900;text-decoration:none}.section-link:hover{color:var(--ui-hover)}
.life-grid{display:grid;grid-template-columns:1.35fr 1fr;gap:13px}.side-stack{display:grid;gap:13px}.dashboard-card{padding:18px;border:1px solid var(--ui-line);border-radius:14px;background:var(--ui-surface-2);min-width:0}.dashboard-card h3{margin:0;font-size:1rem}.card-top{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:14px}.card-type{color:var(--ui-accent);font-size:.63rem;font-weight:900;letter-spacing:.08em;text-transform:uppercase}.news-card{min-height:100%}.news-feature{padding:18px;border:1px solid var(--ui-line);border-radius:12px;background:linear-gradient(135deg,color-mix(in srgb,var(--ui-accent) 12%,var(--ui-surface-3)),var(--ui-surface-3))}.news-feature .news-date{color:var(--ui-accent);font-size:.65rem;font-weight:900;text-transform:uppercase}.news-feature h3{margin:8px 0 8px;font-size:1.28rem}.news-feature p{margin:0;color:var(--ui-soft);font-size:.83rem;line-height:1.55}.news-list{display:grid;margin-top:11px}.news-row{display:grid;grid-template-columns:72px minmax(0,1fr);gap:11px;padding:11px 2px;border-top:1px solid var(--ui-line)}.news-row time{color:var(--ui-muted);font-size:.67rem}.news-row strong{font-size:.78rem;line-height:1.35}
.event-list,.people-list{display:grid}.event-row,.person-row{display:grid;grid-template-columns:50px minmax(0,1fr) auto;align-items:center;gap:11px;padding:10px 0;border-top:1px solid var(--ui-line)}.event-row:first-child,.person-row:first-child{border-top:0;padding-top:0}.event-row:last-child,.person-row:last-child{padding-bottom:0}.date-tile{display:grid;place-items:center;min-height:45px;border:1px solid var(--ui-line-strong);border-radius:9px;background:var(--ui-surface-3);text-align:center}.date-tile strong,.date-tile span{display:block}.date-tile strong{font-size:.86rem}.date-tile span{color:var(--ui-muted);font-size:.55rem;font-weight:900;text-transform:uppercase}.row-copy strong,.row-copy span{display:block}.row-copy strong{font-size:.79rem}.row-copy span{margin-top:3px;color:var(--ui-muted);font-size:.66rem}.row-state{padding:5px 7px;border-radius:999px;background:var(--ui-surface-3);color:var(--ui-secondary);font-size:.57rem;font-weight:900;white-space:nowrap}.row-state.warning{color:var(--ui-warning)}.row-state.success{color:var(--ui-success)}
.game-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:13px}.game-card{position:relative;overflow:hidden;background:linear-gradient(155deg,color-mix(in srgb,var(--ui-accent) 7%,var(--ui-surface-2)),var(--ui-surface-2) 58%);box-shadow:0 14px 32px rgba(0,0,0,.2)}.game-card::before{position:absolute;top:0;right:0;left:0;height:3px;background:var(--ui-accent);box-shadow:0 0 15px color-mix(in srgb,var(--ui-accent) 55%,transparent);content:""}.game-card:nth-child(2)::before{background:#b89aff;box-shadow:0 0 15px rgba(184,154,255,.5)}.game-card h3{margin:5px 0 2px}.game-subtitle{color:var(--ui-muted);font-size:.68rem}.game-score{display:flex;align-items:end;justify-content:space-between;gap:12px;margin:18px 0 12px}.game-score strong{font-size:1.65rem;line-height:1;text-shadow:0 4px 18px rgba(0,0,0,.38)}.game-score span{color:var(--ui-muted);font-size:.66rem;text-align:right}.progress{height:8px;border-radius:999px;background:var(--ui-surface-3);overflow:hidden}.progress>span{display:block;height:100%;border-radius:inherit;background:linear-gradient(90deg,var(--ui-button),var(--ui-accent));box-shadow:0 0 12px color-mix(in srgb,var(--ui-accent) 52%,transparent)}.game-facts{display:grid;gap:7px;margin-top:14px}.game-fact{display:flex;justify-content:space-between;gap:12px;padding-top:7px;border-top:1px solid var(--ui-line);font-size:.68rem}.game-fact span{color:var(--ui-muted)}.game-fact strong{text-align:right}.game-action{display:inline-flex;margin-top:14px;color:var(--ui-navigation);font-size:.69rem;font-weight:900;text-decoration:none}.game-action:hover{color:var(--ui-hover)}
.manage-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:11px}.manage-card{position:relative;display:block;min-height:160px;padding:16px;border:1px solid var(--ui-line);border-radius:13px;background:var(--ui-surface-2);color:inherit;text-decoration:none;transition:border-color .15s ease,box-shadow .15s ease,transform .15s ease}.manage-card:hover{border-color:var(--ui-hover);box-shadow:0 0 18px color-mix(in srgb,var(--ui-hover) 11%,transparent);transform:translateY(-1px)}.console-icon{display:grid;width:38px;height:38px;place-items:center;border:1px solid var(--ui-line-strong);border-radius:10px;background:var(--ui-surface-3);color:var(--ui-accent)}.console-icon svg{width:20px;height:20px}.manage-card h3{margin:12px 0 5px;font-size:.9rem}.manage-card p{margin:0;color:var(--ui-muted);font-size:.7rem;line-height:1.45}.manage-arrow{position:absolute;right:14px;top:13px;color:var(--ui-navigation)}.attention{display:inline-flex;margin-top:10px;padding:5px 7px;border-radius:999px;background:color-mix(in srgb,var(--ui-warning) 11%,var(--ui-surface-3));color:var(--ui-warning);font-size:.58rem;font-weight:900}
@media(max-width:950px){.snapshot-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.life-grid{grid-template-columns:1fr}.side-stack{grid-template-columns:1fr 1fr}.game-grid{grid-template-columns:1fr 1fr}.manage-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}
@media(max-width:900px){.command-hero{grid-template-columns:minmax(0,1fr) minmax(170px,245px);min-height:300px;padding:32px 30px;gap:20px}.command-title{font-size:clamp(2.7rem,7.6vw,4.35rem)}.command-art{min-height:220px}.command-art img{max-height:250px}.command-art-fallback{width:170px;height:170px;font-size:1.35rem}.command-hero::after{left:30px;width:190px}}
@media(max-width:650px){.command-hero{grid-template-columns:1fr;padding:21px 18px 18px}.command-art{position:absolute;right:-20px;bottom:-18px;width:185px;opacity:.25}.command-copy{padding-right:3px}.command-title{max-width:390px;font-size:clamp(2.05rem,12vw,3rem)}.command-sub{max-width:88%;font-size:.78rem}.preview-note,.section-head{display:block}.sample-badge{display:inline-flex;margin-top:8px}.snapshot-grid,.side-stack,.game-grid,.manage-grid{grid-template-columns:1fr}.snapshot-card strong{font-size:1.3rem}.event-row,.person-row{grid-template-columns:46px minmax(0,1fr)}.row-state{grid-column:2;width:max-content}.news-row{grid-template-columns:62px minmax(0,1fr)}}
.command-hero{grid-template-columns:minmax(0,1fr) minmax(190px,270px);gap:24px;min-height:245px;margin-bottom:14px;padding:27px 34px}.command-hero::after{left:34px;width:175px;height:3px}.command-kicker{margin-bottom:7px;font-size:.64rem}.command-title{max-width:720px;font-size:clamp(2.45rem,4.7vw,4.2rem);line-height:.9}.command-sub{max-width:690px;margin-top:12px;font-size:.82rem}.command-meta{display:none}.command-art{min-height:190px}.command-art img{width:min(100%,255px);max-height:220px}.command-art-fallback{width:160px;height:160px;font-size:1.3rem}
.intelligence-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr)) minmax(255px,.78fr);gap:13px}.leader-list{display:grid;margin-top:12px}.leader-row{display:grid;grid-template-columns:26px minmax(0,1fr) auto;align-items:center;gap:9px;padding:8px 0;border-top:1px solid var(--ui-line)}.leader-row:first-child{border-top:0}.leader-rank{display:grid;width:24px;height:24px;place-items:center;border-radius:7px;background:var(--ui-surface-3);color:var(--ui-accent);font-size:.64rem;font-weight:950}.leader-row:nth-child(1) .leader-rank{background:color-mix(in srgb,var(--ui-user-rank) 16%,var(--ui-surface-3));color:var(--ui-user-rank)}.leader-name{overflow:hidden;font-size:.74rem;font-weight:850;text-overflow:ellipsis;white-space:nowrap}.leader-value{color:var(--ui-soft);font-size:.7rem;font-weight:900}.leader-more{margin-top:5px;color:var(--ui-muted);font-size:.64rem}.shield-card{border-color:rgba(255,107,120,.34);background:radial-gradient(circle at 100% 0,rgba(255,107,120,.16),transparent 46%),var(--ui-surface-2)}.shield-total{display:flex;align-items:end;justify-content:space-between;gap:12px;margin:13px 0 7px}.shield-total strong{color:#ff7a86;font-size:1.65rem;line-height:1}.shield-total span{color:var(--ui-muted);font-size:.64rem;text-align:right}.shield-note{margin:0 0 10px;color:var(--ui-muted);font-size:.68rem}.shield-list{display:grid}.shield-row{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:8px 0;border-top:1px solid var(--ui-line);font-size:.69rem}.shield-player{display:flex;align-items:center;gap:7px;font-weight:850}.shield-player::before{width:7px;height:7px;border-radius:50%;background:#ff6b78;box-shadow:0 0 9px rgba(255,107,120,.55);content:""}.shield-row span:last-child{color:#ff9aa3;font-size:.61rem;font-weight:900;text-transform:uppercase}
.happening-panel{padding:11px}.happening-rail{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.happening-item{min-width:0;padding:11px 12px;border:1px solid var(--ui-line);border-radius:10px;background:var(--ui-surface-3)}.happening-item-top{display:flex;align-items:center;justify-content:space-between;gap:8px}.happening-type{font-size:.57rem;font-weight:950;letter-spacing:.06em;text-transform:uppercase}.happening-type.event{color:var(--ui-accent)}.happening-type.birthday{color:var(--ui-user-rank)}.happening-type.away{color:var(--ui-warning)}.happening-when{color:var(--ui-muted);font-size:.58rem;font-weight:800}.happening-item strong,.happening-item span{display:block}.happening-item>strong{overflow:hidden;margin-top:7px;font-size:.73rem;text-overflow:ellipsis;white-space:nowrap}.happening-item>span{overflow:hidden;margin-top:3px;color:var(--ui-muted);font-size:.64rem;text-overflow:ellipsis;white-space:nowrap}.discord-item{border-color:rgba(137,146,255,.42);background:radial-gradient(circle at 100% 0,rgba(88,101,242,.28),transparent 55%),#151a36}.discord-item .happening-type{color:#aeb5ff}.discord-join{display:inline-flex;align-items:center;justify-content:center;margin-top:9px;padding:6px 9px;border:1px solid rgba(137,146,255,.5);border-radius:8px;background:#5865f2;color:#fff;font-size:.62rem;font-weight:900;text-decoration:none}.discord-join:hover{background:#6975f4}.card-empty{display:grid;min-height:126px;place-content:center;padding:18px;text-align:center}.card-empty strong,.card-empty span{display:block}.card-empty strong{font-size:.82rem}.card-empty span{max-width:250px;margin-top:6px;color:var(--ui-muted);font-size:.68rem;line-height:1.45}.game-empty{min-height:172px}.snapshot-card.empty strong{color:var(--ui-muted)}
@media(max-width:950px){.command-hero{grid-template-columns:minmax(0,1fr) minmax(155px,215px);min-height:220px;padding:24px 26px}.command-title{font-size:clamp(2.35rem,6vw,3.65rem)}.command-art{min-height:170px}.command-art img{max-height:190px}.command-art-fallback{width:140px;height:140px}.intelligence-grid{grid-template-columns:1fr 1fr}.shield-card{grid-column:1/-1}.happening-rail{grid-template-columns:1fr 1fr}}
@media(max-width:650px){.command-hero{grid-template-columns:1fr;min-height:215px;padding:21px 18px}.command-hero::after{left:18px}.command-title{font-size:clamp(2.05rem,11vw,3rem)}.command-art{position:absolute;right:-20px;bottom:-22px;width:175px;min-height:170px;opacity:.28}.command-art img{max-height:175px}.command-sub{max-width:82%}.intelligence-grid,.happening-rail{grid-template-columns:1fr}.shield-card{grid-column:auto}.happening-item>strong,.happening-item>span{white-space:normal}}
.home-roster-card{overflow:hidden;padding:0}.home-roster-head{display:flex;align-items:end;justify-content:space-between;gap:16px;padding:17px 18px;border-bottom:1px solid var(--ui-line)}.home-roster-head h3{margin:0;font-size:1rem}.home-roster-head p{margin:4px 0 0;color:var(--ui-muted);font-size:.7rem}.home-roster-head a{color:var(--ui-navigation);font-size:.69rem;font-weight:900;text-decoration:none;white-space:nowrap}.home-roster-head a:hover{color:var(--ui-hover)}.home-roster-scroll{overflow-x:auto}.home-roster{width:100%;border-collapse:collapse;font-size:.73rem}.home-roster th{padding:9px 12px;background:var(--ui-surface-3);color:var(--ui-muted);font-size:.59rem;font-weight:950;letter-spacing:.06em;text-align:left;text-transform:uppercase;white-space:nowrap}.home-roster td{padding:9px 12px;border-top:1px solid var(--ui-line);vertical-align:middle;white-space:nowrap}.home-roster tbody tr:first-child td{border-top:0}.home-roster tbody tr:hover{background:color-mix(in srgb,var(--ui-hover) 5%,transparent)}.home-roster .number{text-align:right}.home-roster-name{font-weight:900}.home-roster-name a{color:inherit;text-decoration:none}.home-roster-name a:hover{color:var(--ui-hover)}.home-rank{display:inline-grid;min-width:29px;padding:5px 7px;place-items:center;border:1px solid color-mix(in srgb,var(--ui-user-rank) 35%,var(--ui-line));border-radius:7px;background:color-mix(in srgb,var(--ui-user-rank) 9%,var(--ui-surface-3));color:var(--ui-user-rank);font-size:.63rem;font-weight:950}.home-movement{font-size:.65rem;font-weight:900}.home-movement.up{color:var(--ui-success)}.home-movement.down{color:var(--ui-danger)}.home-movement.same{color:var(--ui-muted)}
@media(max-width:650px){.home-roster-head{align-items:flex-start;padding:15px}.home-roster th,.home-roster td{padding:8px 10px}.home-roster th:nth-child(5),.home-roster td:nth-child(5){display:none}}
</style>`;

const demoCss=`<style>
.demo-banner{display:flex;align-items:center;justify-content:space-between;gap:14px;margin-bottom:14px;padding:10px 13px;border:1px solid rgba(245,196,81,.4);border-radius:10px;background:rgba(245,196,81,.08);color:#f5d985;font-size:.7rem}.demo-banner strong{color:#ffe9a8}.demo-banner a{font-weight:900;text-decoration:none}.classic-head{display:grid;grid-template-columns:minmax(0,1.2fr) repeat(3,minmax(125px,.42fr));overflow:hidden;margin-bottom:12px;border:1px solid var(--ui-line);border-radius:14px;background:linear-gradient(110deg,#0a2943,#071522 57%)}.classic-brand{padding:23px 25px}.classic-brand span{color:var(--ui-accent);font-size:.61rem;font-weight:950;letter-spacing:.12em;text-transform:uppercase}.classic-brand h1{margin:5px 0 0;font-size:clamp(1.7rem,3.3vw,2.8rem);line-height:.95}.classic-brand p{margin:8px 0 0;color:var(--ui-muted);font-size:.7rem}.classic-stat{display:grid;align-content:center;padding:16px;border-left:1px solid var(--ui-line)}.classic-stat span,.classic-stat strong,.classic-stat small{display:block}.classic-stat span{color:var(--ui-muted);font-size:.57rem;font-weight:900;text-transform:uppercase}.classic-stat strong{margin-top:5px;font-size:1.26rem}.classic-stat small{margin-top:5px;font-size:.61rem}.movement{font-weight:950}.movement.up{color:var(--ui-success)}.movement.down{color:var(--ui-danger)}.movement.same{color:var(--ui-muted)}.classic-grid{display:grid;grid-template-columns:minmax(0,1.42fr) minmax(280px,.78fr);gap:12px}.classic-stack{display:grid;gap:12px}.classic-card{padding:14px;border:1px solid var(--ui-line);border-radius:13px;background:var(--ui-surface-2)}.classic-title{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:9px}.classic-title h2{margin:0;font-size:.9rem}.classic-title span,.classic-title a{color:var(--ui-muted);font-size:.61rem;font-weight:850;text-decoration:none}.roster-table{width:100%;border-collapse:collapse}.roster-table th,.roster-table td{padding:7px 8px;border-top:1px solid var(--ui-line);font-size:.67rem;text-align:left}.roster-table th{border-top:0;color:var(--ui-muted);font-size:.55rem;text-transform:uppercase}.roster-table .number{text-align:right}.roster-name{font-weight:900}.rank-chip{display:inline-grid;min-width:28px;padding:4px 5px;place-items:center;border-radius:6px;background:var(--ui-surface-3);color:var(--ui-user-rank);font-size:.59rem;font-weight:950}.mini-panels{display:grid;grid-template-columns:1fr 1fr;gap:12px}.mini-panel{min-height:145px}.train-display{margin-top:15px}.train-display span,.train-display strong{display:block}.train-display span{color:var(--ui-muted);font-size:.59rem;font-weight:900;text-transform:uppercase}.train-display strong{margin-top:4px;font-size:1.04rem}.train-display .vip{margin-top:11px;font-size:.86rem}.shield-visual{position:relative;overflow:hidden;border-color:rgba(255,107,120,.38);background:radial-gradient(circle at 100% 0,rgba(255,107,120,.18),transparent 50%),var(--ui-surface-2)}.shield-score{display:flex;align-items:end;justify-content:space-between;margin:10px 0}.shield-score strong{color:#ff7a86;font-size:2rem;line-height:1}.shield-score span{color:var(--ui-muted);font-size:.61rem;text-align:right}.shield-dots{display:flex;gap:5px;margin-bottom:9px}.shield-dots i{width:8px;height:8px;border-radius:50%;background:#ff6b78;box-shadow:0 0 8px rgba(255,107,120,.5)}.change-list{display:grid}.change-row{display:grid;grid-template-columns:9px minmax(0,1fr) auto;gap:9px;padding:9px 0;border-top:1px solid var(--ui-line)}.change-row:first-child{border-top:0}.change-dot{width:8px;height:8px;margin-top:4px;border-radius:50%;background:var(--ui-accent);box-shadow:0 0 8px color-mix(in srgb,var(--ui-accent) 55%,transparent)}.change-row.joined .change-dot{background:var(--ui-success)}.change-row.left .change-dot{background:var(--ui-danger)}.change-copy strong,.change-copy span{display:block}.change-copy strong{font-size:.69rem}.change-copy span{margin-top:2px;color:var(--ui-muted);font-size:.6rem}.change-date{color:var(--ui-muted);font-size:.57rem}.classic-happening{margin-top:12px}.classic-happening .happening-rail{grid-template-columns:repeat(4,minmax(0,1fr))}@media(max-width:900px){.classic-head{grid-template-columns:1fr 1fr 1fr}.classic-brand{grid-column:1/-1}.classic-stat:first-of-type{border-left:0}.classic-grid{grid-template-columns:1fr}.classic-stack{grid-template-columns:1fr 1fr}.classic-stack .classic-card:first-child{grid-column:1/-1}}@media(max-width:650px){.demo-banner{display:block}.demo-banner a{display:block;margin-top:6px}.classic-head{grid-template-columns:1fr}.classic-brand{grid-column:auto}.classic-stat{border-top:1px solid var(--ui-line);border-left:0}.classic-stack,.mini-panels,.classic-happening .happening-rail{grid-template-columns:1fr}.classic-stack .classic-card:first-child{grid-column:auto}.roster-table th:nth-child(3),.roster-table td:nth-child(3){display:none}}
</style>`;

export async function renderUiV2Console(env:UiV2Env,context:UiV2Context,demo=false){
  const today=londonToday();
  const [activePlayers,awayResult,settingRows,weeklyEvent,powerSummary,heroRankings,squadRankings,birthday,train,vsSummary,dsSummary,shieldSummary,demoRoster,allianceChanges]=await Promise.all([
    env.DB.prepare("SELECT COUNT(*) total FROM players WHERE is_active=1").first<CountRow>(),
    env.DB.prepare("SELECT p.display_name FROM players p WHERE p.is_active=1 AND EXISTS(SELECT 1 FROM player_away_periods w WHERE w.player_id=p.id AND w.cancelled_at IS NULL AND w.start_date<=? AND (w.end_date IS NULL OR w.end_date>=?)) ORDER BY p.display_name COLLATE NOCASE LIMIT 4").bind(today,today).all<AwayRow>().catch(()=>null),
    env.DB.prepare("SELECT key,value FROM settings WHERE key='discord_invite_url'").all<SettingRow>().catch(()=>null),
    env.DB.prepare("SELECT event_name,event_time,note FROM weekly_events WHERE day_of_week=? AND enabled=1 AND TRIM(event_name)<>'' LIMIT 1").bind(londonWeekday()).first<WeeklyEventRow>().catch(()=>null),
    env.DB.prepare("SELECT SUM(player_power) alliance_power,COUNT(player_power) power_players FROM players WHERE is_active=1 AND player_power IS NOT NULL").first<PowerSummaryRow>().catch(()=>null),
    env.DB.prepare("SELECT display_name,total_hero_power power FROM players WHERE is_active=1 AND total_hero_power IS NOT NULL ORDER BY total_hero_power DESC,display_name COLLATE NOCASE LIMIT 10").all<RankingRow>().catch(()=>null),
    env.DB.prepare("SELECT p.display_name,s.power FROM player_squads s JOIN players p ON p.id=s.player_id WHERE p.is_active=1 AND s.squad_number=1 AND s.power IS NOT NULL ORDER BY s.power DESC,p.display_name COLLATE NOCASE LIMIT 20").all<RankingRow>().catch(()=>null),
    env.DB.prepare("SELECT display_name,birthday_month,birthday_day FROM players WHERE is_active=1 AND birthday_month IS NOT NULL AND birthday_day IS NOT NULL ORDER BY CASE WHEN printf('%02d-%02d',birthday_month,birthday_day)>=strftime('%m-%d','now') THEN 0 ELSE 1 END,printf('%02d-%02d',birthday_month,birthday_day) LIMIT 1").first<BirthdayRow>().catch(()=>null),
    context.user.canViewFrontTrain?env.DB.prepare("SELECT d.display_name driver_name,v.display_name vip_name FROM alliance_train_schedule t JOIN players d ON d.id=t.driver_player_id LEFT JOIN players v ON v.id=t.vip_player_id WHERE t.schedule_date=? LIMIT 1").bind(today).first<TrainRow>().catch(()=>null):Promise.resolve(null),
    context.user.canViewVs?env.DB.prepare("SELECT d.competition_date,d.challenge_name,d.daily_target,COALESCE(SUM(s.points),0) total,COUNT(s.player_id) participants,COALESCE(SUM(CASE WHEN s.points>=d.daily_target THEN 1 ELSE 0 END),0) hit_target FROM vs_days d LEFT JOIN vs_scores s ON s.competition_date=d.competition_date GROUP BY d.competition_date ORDER BY d.competition_date DESC LIMIT 1").first<VsSummaryRow>().catch(()=>null):Promise.resolve(null),
    context.user.canViewDs?env.DB.prepare("SELECT e.*,(SELECT COUNT(*) FROM ds_event_players p WHERE p.event_id=e.id AND p.team='A' AND p.selection_status='selected') a_selected,(SELECT COUNT(*) FROM ds_event_players p WHERE p.event_id=e.id AND p.team='A' AND p.selection_status='substitute') a_subs,(SELECT COUNT(*) FROM ds_event_players p WHERE p.event_id=e.id AND p.team='A' AND p.participated=1) a_played,(SELECT COUNT(*) FROM ds_event_players p WHERE p.event_id=e.id AND p.team='B' AND p.selection_status='selected') b_selected,(SELECT COUNT(*) FROM ds_event_players p WHERE p.event_id=e.id AND p.team='B' AND p.selection_status='substitute') b_subs,(SELECT COUNT(*) FROM ds_event_players p WHERE p.event_id=e.id AND p.team='B' AND p.participated=1) b_played FROM ds_events e ORDER BY e.battle_date DESC LIMIT 1").first<DsSummaryRow>().catch(()=>null):Promise.resolve(null),
    context.user.canViewFrontShieldDrops?env.DB.prepare("SELECT war_date,COUNT(*) total,COUNT(DISTINCT player_id) affected FROM shield_drop_incidents WHERE war_date=(SELECT MAX(war_date) FROM shield_drop_incidents) GROUP BY war_date").first<ShieldSummaryRow>().catch(()=>null):Promise.resolve(null),
    env.DB.prepare("SELECT p.id,p.display_name,p.rank,p.base_level,p.player_power,(SELECT s.power FROM player_squads s WHERE s.player_id=p.id AND s.squad_number=1 LIMIT 1) squad_1_power,p.total_hero_power,(SELECT s.total_strength FROM player_roster_snapshots s WHERE s.player_id=p.id AND s.total_strength IS NOT NULL ORDER BY s.recorded_at DESC,s.id DESC LIMIT 1 OFFSET 1) previous_power FROM players p WHERE p.is_active=1 ORDER BY p.rank DESC,p.display_name COLLATE NOCASE LIMIT 100").all<DemoRosterRow>().catch(()=>null),
    demo?env.DB.prepare("SELECT display_name,change_type,detail,change_date FROM (SELECT p.display_name,'rank' change_type,'R'||COALESCE(h.old_rank,'?')||' → R'||h.new_rank detail,h.changed_at change_date FROM player_rank_history h JOIN players p ON p.id=h.player_id WHERE h.old_rank IS NOT NULL UNION ALL SELECT display_name,'joined','Joined the alliance',COALESCE(joined_at,created_at) FROM players WHERE joined_at IS NOT NULL UNION ALL SELECT display_name,'left','Moved to former players',left_at FROM players WHERE left_at IS NOT NULL) ORDER BY change_date DESC LIMIT 8").all<AllianceChangeRow>().catch(()=>null):Promise.resolve(null)
  ]);
  const shieldPlayers=shieldSummary?(await env.DB.prepare("SELECT p.display_name,COUNT(*) drop_count FROM shield_drop_incidents i JOIN players p ON p.id=i.player_id WHERE i.war_date=? GROUP BY i.player_id,p.display_name ORDER BY drop_count DESC,p.display_name COLLATE NOCASE LIMIT 8").bind(shieldSummary.war_date).all<ShieldPlayerRow>().catch(()=>null))?.results??[]:[];
  const active=Number(activePlayers?.total??0),awayPlayers=awayResult?.results??[];
  const settings=Object.fromEntries((settingRows?.results??[]).map(row=>[row.key,row.value]));
  const discordInvite=settings.discord_invite_url?.trim()||"";
  const eventName=weeklyEvent?.event_name?.trim()||"No event scheduled",eventWhen=weeklyEvent?.event_time?.trim()||"Today",eventNote=weeklyEvent?.note?.trim()||"Nothing planned for today";
  const awaySummary=awayPlayers.length?`${awayPlayers.slice(0,2).map(player=>esc(player.display_name)).join(", ")}${awayPlayers.length>2?` +${awayPlayers.length-2}`:""}`:"Nobody currently away";
  const commandArtwork=context.branding.mainLogo?`<img src="/assets/${encodeURIComponent(context.branding.mainLogo)}" alt="${esc(context.alliance.name)} crest">`:`<div class="command-art-fallback">${esc(context.alliance.tag?`[${context.alliance.tag}]`:"AMP")}</div>`;
  const heroRows=heroRankings?.results??[],squadRows=squadRankings?.results??[];
  const rankingsCards=context.user.canViewFrontRankings?`${rankingCard("Top 10","Total Hero Power",heroRows,10)}${rankingCard("Top 20","Squad 1 Power",squadRows,20)}`:"";
  const shieldCard=context.user.canViewFrontShieldDrops?`<article class="dashboard-card shield-card"><span class="card-type">Saturday war day${shieldSummary?` · ${esc(shieldSummary.war_date)}`:""}</span><h3>Shield Drop Watch</h3>${shieldSummary?`<div class="shield-total"><strong>${shieldSummary.total}</strong><span>${shieldSummary.total===1?"incident":"incidents"} · ${shieldSummary.affected} ${shieldSummary.affected===1?"player":"players"}</span></div><div class="shield-players">${shieldPlayers.map(row=>`<div class="shield-row"><span class="shield-player">${esc(row.display_name)}</span><strong>${row.drop_count} ${row.drop_count===1?"drop":"drops"}</strong></div>`).join("")}</div>${context.user.canManageShieldDrops?'<a class="game-action" href="/ui-v2/shield-drops">Open Shield Drop Watch →</a>':""}`:`<div class="card-empty"><strong>No shield drops recorded</strong><span>The latest Saturday incidents will appear here.</span></div>${context.user.canManageShieldDrops?'<a class="game-action" href="/ui-v2/shield-drops">Record first incident →</a>':""}`}</article>`:"";
  const intelligenceSection=rankingsCards||shieldCard?`<section class="dashboard-section"><div class="section-head"><div><h2>Alliance intelligence</h2><p>The rankings and Saturday war-day checks that matter most.</p></div></div><div class="intelligence-grid">${rankingsCards}${shieldCard}</div></section>`:"";
  const alliancePower=powerSummary?.alliance_power??null,averagePower=alliancePower!==null&&powerSummary?.power_players?alliancePower/powerSummary.power_players:null;
  const birthdayText=birthday?.display_name||"No birthday recorded",birthdayNote=birthday?`${String(birthday.birthday_day).padStart(2,"0")}/${String(birthday.birthday_month).padStart(2,"0")}`:"Add birthdays on player profiles";
  const awayTile=context.user.canViewFrontAwayDetails?`<div class="happening-item"><div class="happening-item-top"><span class="happening-type away">Away</span><span class="happening-when">Now</span></div><strong>${awaySummary}</strong><span>${awayPlayers.length?"Currently unavailable":"All members available"}</span></div>`:"";
  const homeRoster=demoRoster?.results??[];
  const homeMovement=(current:number|null,previous:number|null)=>{if(current===null||previous===null)return '<span class="home-movement same">No earlier import</span>';const difference=current-previous;if(!difference)return '<span class="home-movement same">— No change</span>';return `<span class="home-movement ${difference>0?"up":"down"}">${difference>0?"▲":"▼"} ${formatPower(Math.abs(difference))}</span>`};
  const homeRosterRows=homeRoster.map(player=>{const name=context.user.canManagePlayers?`<a href="/ui-v2/players/${player.id}">${esc(player.display_name)}</a>`:esc(player.display_name);return `<tr><td><span class="home-rank">R${player.rank}</span></td><td class="home-roster-name">${name}</td><td>${player.base_level??"—"}</td><td class="number">${formatPower(player.player_power)}</td><td class="number">${homeMovement(player.player_power,player.previous_power)}</td><td class="number">${formatPower(player.squad_1_power)}</td><td class="number">${formatPower(player.total_hero_power)}</td></tr>`}).join("");
  const homeRosterSection=`<section class="dashboard-section"><div class="section-head"><div><h2>Alliance roster</h2><p>The current team and latest power figures at a glance.</p></div></div><article class="dashboard-card home-roster-card"><div class="home-roster-head"><div><h3>${active} active players</h3><p>Sorted by rank, then player name.</p></div>${context.user.canManagePlayers?'<a href="/ui-v2/players">Open player management →</a>':""}</div><div class="home-roster-scroll"><table class="home-roster"><thead><tr><th>Rank</th><th>Player</th><th>HQ</th><th class="number">Total strength</th><th class="number">Since last import</th><th class="number">Squad 1</th><th class="number">Hero power</th></tr></thead><tbody>${homeRosterRows||'<tr><td colspan="7">No active roster data is available yet.</td></tr>'}</tbody></table></div></article></section>`;
  if(demo){
    const roster=demoRoster?.results??[],changes=allianceChanges?.results??[];
    const comparable=roster.filter(row=>row.player_power!==null&&row.previous_power!==null),previousAlliance=comparable.length?comparable.reduce((sum,row)=>sum+Number(row.previous_power),0):null,currentComparable=comparable.length?comparable.reduce((sum,row)=>sum+Number(row.player_power),0):null;
    const change=(current:number|null,previous:number|null)=>{if(current===null||previous===null)return '<span class="movement same">No earlier import</span>';const difference=current-previous;if(!difference)return '<span class="movement same">— No change</span>';return `<span class="movement ${difference>0?"up":"down"}">${difference>0?"▲":"▼"} ${formatPower(Math.abs(difference))}</span>`};
    const powerChange=currentComparable===null?change(null,null):change(currentComparable,previousAlliance),currentAverage=currentComparable===null?null:currentComparable/comparable.length,previousAverage=previousAlliance===null?null:previousAlliance/comparable.length;
    const rosterRows=roster.map(row=>`<tr><td><span class="rank-chip">R${row.rank}</span></td><td class="roster-name">${esc(row.display_name)}</td><td>${row.base_level??"—"}</td><td class="number">${formatPower(row.player_power)}</td><td class="number">${change(row.player_power,row.previous_power)}</td></tr>`).join("");
    const changeRows=changes.map(row=>`<div class="change-row ${esc(row.change_type)}"><i class="change-dot"></i><div class="change-copy"><strong>${esc(row.display_name)}</strong><span>${esc(row.detail)}</span></div><time class="change-date">${esc(row.change_date.slice(0,10).split("-").reverse().join("/"))}</time></div>`).join("");
    const demoBody=`${dashboardCss}${demoCss}<div class="demo-banner"><span><strong>Home concept demo</strong> · Compact like the original site, powered by the new database.</span><a href="/ui-v2">Return to current Home →</a></div>
      <section class="classic-head"><div class="classic-brand"><span>${esc(context.alliance.tag?`[${context.alliance.tag}] · `:"")}Server ${esc(context.alliance.serverNumber||"—")}</span><h1>${esc(context.alliance.name)}</h1><p>Alliance overview · latest roster import compared with the previous one</p></div><div class="classic-stat"><span>Members</span><strong>${active}</strong><small>Current roster</small></div><div class="classic-stat"><span>Alliance power</span><strong>${formatPower(alliancePower)}</strong><small>${powerChange}</small></div><div class="classic-stat"><span>Average power</span><strong>${formatPower(averagePower)}</strong><small>${change(currentAverage,previousAverage)}</small></div></section>
      <div class="classic-grid"><section class="classic-card"><div class="classic-title"><h2>Alliance roster</h2><a href="/ui-v2/players">View all players →</a></div><table class="roster-table"><thead><tr><th>Rank</th><th>Player</th><th>Base</th><th class="number">Power</th><th class="number">Since last import</th></tr></thead><tbody>${rosterRows||'<tr><td colspan="5">No roster data available.</td></tr>'}</tbody></table></section><aside class="classic-stack">
        <section class="classic-card"><div class="classic-title"><h2>Alliance changes</h2><span>Latest activity</span></div><div class="change-list">${changeRows||'<div class="card-empty"><strong>No alliance changes yet</strong><span>Joins, departures and rank moves will appear here.</span></div>'}</div></section>
        <div class="mini-panels">${context.user.canViewFrontTrain?`<section class="classic-card mini-panel"><div class="classic-title"><h2>Today’s Train</h2><span>${esc(today.split("-").reverse().join("/"))}</span></div><div class="train-display"><span>Driver</span><strong>${esc(train?.driver_name||"Not scheduled")}</strong><span class="vip">VIP</span><strong class="vip">${esc(train?.vip_name||"Not selected")}</strong></div></section>`:""}${context.user.canViewFrontShieldDrops?`<section class="classic-card mini-panel shield-visual"><div class="classic-title"><h2>Shield Drop</h2><span>${shieldSummary?esc(shieldSummary.war_date.split("-").reverse().join("/")):"Latest war"}</span></div>${shieldSummary?`<div class="shield-score"><strong>${shieldSummary.total}</strong><span>${shieldSummary.affected} players<br>affected</span></div><div class="shield-dots">${Array.from({length:Math.min(shieldSummary.total,12)},()=>"<i></i>").join("")}</div><span class="game-subtitle">${esc(shieldPlayers.slice(0,3).map(row=>row.display_name).join(", "))}${shieldPlayers.length>3?` +${shieldPlayers.length-3} more`:""}</span>`:'<div class="card-empty"><strong>No drops recorded</strong></div>'}</section>`:""}</div>
      </aside></div>
      <section class="classic-card classic-happening"><div class="classic-title"><h2>What’s happening</h2><span>One quick look at alliance life</span></div><div class="happening-rail">${discordInvite?`<div class="happening-item discord-item"><div class="happening-item-top"><span class="happening-type">Discord</span><span class="happening-when">Community</span></div><strong>Join the conversation</strong><span>Chat, news and war coordination.</span><a class="discord-join" href="${esc(discordInvite)}" target="_blank" rel="noopener">Join Discord →</a></div>`:""}<div class="happening-item"><div class="happening-item-top"><span class="happening-type event">Event</span><span class="happening-when">${esc(eventWhen)}</span></div><strong>${esc(eventName)}</strong><span>${esc(eventNote)}</span></div><div class="happening-item"><div class="happening-item-top"><span class="happening-type birthday">Birthday</span><span class="happening-when">Next</span></div><strong>${esc(birthdayText)}</strong><span>${esc(birthdayNote)}</span></div>${awayTile}</div></section>`;
    return uiV2Html(renderUiV2FrontShell(context,{title:"Home Concept Demo",body:demoBody,activePath:"/ui-v2"}));
  }
  const body=`${dashboardCss}
    <section class="command-hero" aria-label="${esc(context.alliance.name)} command centre"><div class="command-copy"><div class="command-kicker">Alliance intelligence</div><h2 class="command-title">${esc(context.alliance.name)}<span class="gold">Command Centre</span></h2><p class="command-sub">Events, member activity and the latest game performance in one place.</p></div><div class="command-art">${commandArtwork}</div></section>
    <div class="snapshot-grid">
      <div class="snapshot-card"><span>Alliance members</span><strong>${active}</strong><small>Current roster</small></div>
      <div class="snapshot-card${alliancePower===null?" empty":""}"><span>Alliance power</span><strong>${formatPower(alliancePower)}</strong><small>${powerSummary?.power_players?`${powerSummary.power_players} players with power recorded`:"Awaiting roster power data"}</small></div>
      <div class="snapshot-card${averagePower===null?" empty":""}"><span>Average power</span><strong>${formatPower(averagePower)}</strong><small>${averagePower!==null?"Per player with power recorded":"Awaiting roster power data"}</small></div>
      ${context.user.canViewFrontTrain?`<div class="snapshot-card${train?"":" empty"}"><span>Today’s Train</span><strong>Driver: ${esc(train?.driver_name||"Not scheduled")}</strong><small class="train-vip">VIP: ${train?.vip_name?esc(train.vip_name):train?"Not selected":"No entry for today"}</small></div>`:""}
    </div>
    ${homeRosterSection}
    <section class="dashboard-section">
      <div class="section-head"><div><h2>Game performance</h2><p>The latest alliance activity and competition statistics.</p></div></div>
      <div class="game-grid">
        ${context.user.canViewVs?`<article class="dashboard-card game-card"><span class="card-type">VS Battle Centre</span><h3>Alliance contribution</h3>${vsSummary?`<span class="game-subtitle">${esc(vsSummary.competition_date)} · ${esc(vsSummary.challenge_name)}</span><div class="game-score"><strong>${formatPower(vsSummary.total)}</strong><span>Our alliance<br>total</span></div><div class="game-facts"><div class="game-fact"><span>Players entered</span><strong>${vsSummary.participants}</strong></div><div class="game-fact"><span>Players over ${formatPower(vsSummary.daily_target)}</span><strong>${vsSummary.hit_target}</strong></div></div><a class="game-action" href="/ui-v2/vs">Open VS details →</a>`:'<div class="card-empty game-empty"><strong>No VS results yet</strong><span>Enter the first competition day in the VS Battle Centre.</span></div>'}</article>`:""}
        ${context.user.canViewDs?`<article class="dashboard-card game-card"><span class="card-type">Desert Storm</span><h3>Team A &amp; Team B</h3>${dsSummary?`<span class="game-subtitle">${esc(dsSummary.battle_date)}${dsSummary.battle_time?` · ${esc(dsSummary.battle_time)}`:""}</span><div class="game-facts"><div class="game-fact"><span>Team A vs ${esc(dsSummary.team_a_opponent)}</span><strong>${dsSummary.results_finalised_at?`${dsSummary.team_a_score}–${dsSummary.team_a_enemy_score} · ${dsSummary.a_played} played`:`${dsSummary.a_selected} selected · ${dsSummary.a_subs} subs`}</strong></div><div class="game-fact"><span>Team B vs ${esc(dsSummary.team_b_opponent)}</span><strong>${dsSummary.results_finalised_at?`${dsSummary.team_b_score}–${dsSummary.team_b_enemy_score} · ${dsSummary.b_played} played`:`${dsSummary.b_selected} selected · ${dsSummary.b_subs} subs`}</strong></div></div><a class="game-action" href="/ui-v2/desert-storm">Open Desert Storm →</a>`:'<div class="card-empty game-empty"><strong>No Desert Storm teams yet</strong><span>Create the Thursday selection in Leadership.</span></div>'}</article>`:""}
      </div>
    </section>
    ${intelligenceSection}
    <section class="dashboard-section">
      <div class="section-head"><div><h2>What’s happening</h2><p>One quick look at alliance life.</p></div></div>
      <article class="dashboard-card happening-panel"><div class="happening-rail">${discordInvite?`<div class="happening-item discord-item"><div class="happening-item-top"><span class="happening-type">Alliance Discord</span><span class="happening-when">Community</span></div><strong>Join ${esc(context.alliance.name)}</strong><span>Chat, announcements and war coordination.</span><a class="discord-join" href="${esc(discordInvite)}" target="_blank" rel="noopener">Join Discord →</a></div>`:""}<div class="happening-item"><div class="happening-item-top"><span class="happening-type event">Event</span><span class="happening-when">${esc(eventWhen)}</span></div><strong>${esc(eventName)}</strong><span>${esc(eventNote)}</span></div><div class="happening-item"><div class="happening-item-top"><span class="happening-type birthday">Birthday</span><span class="happening-when">Next</span></div><strong>${esc(birthdayText)}</strong><span>${esc(birthdayNote)}</span></div>${awayTile}</div></article>
    </section>`;
  return uiV2Html(renderUiV2FrontShell(context,{title:"Command Centre",body,activePath:"/ui-v2"}));
}
