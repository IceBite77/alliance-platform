import app from "./security_access";
interface Env { DB:D1Database; ASSETS:R2Bucket; APP_URL:string; DISCORD_CLIENT_ID:string; DISCORD_CLIENT_SECRET:string; DISCORD_BOT_TOKEN:string; SETUP_KEY:string; AUTH_SECRET:string; }
type Branding={brand_main_logo?:string;brand_login_logo?:string;brand_favicon?:string;footer_text?:string;theme_accent?:string;theme_icon?:string};
type HomeIdentity={display_name:string;rank:number;rank_name:string|null;show_rank_names:string|null;is_owner:number};
const SESSION_COOKIE="ap_session";
const esc=(v:string)=>v.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");
const withHtml=(res:Response,html:string)=>{const headers=new Headers(res.headers);headers.delete("content-length");return new Response(html,{status:res.status,statusText:res.statusText,headers})};
const cookie=(r:Request,n:string)=>{for(const p of(r.headers.get("cookie")??"").split(";")){const [x,...z]=p.trim().split("=");if(x===n)return z.join("=")}return null};
const b64=(b:Uint8Array)=>{let s="";for(const x of b)s+=String.fromCharCode(x);return btoa(s).replaceAll("+","-").replaceAll("/","_").replaceAll("=","")};
const hash=async(v:string)=>b64(new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v))));
const homeIdentity=async(r:Request,e:Env)=>{const t=cookie(r,SESSION_COOKIE);if(!t)return null;return e.DB.prepare(`SELECT p.display_name,p.rank,ar.display_name AS rank_name,(SELECT value FROM settings WHERE key='show_rank_names' LIMIT 1) AS show_rank_names,a.is_owner FROM sessions s JOIN accounts a ON a.id=s.account_id JOIN players p ON p.id=a.player_id LEFT JOIN alliance_ranks ar ON ar.rank_level=p.rank WHERE s.token_hash=? AND s.revoked_at IS NULL AND s.expires_at>CURRENT_TIMESTAMP AND a.is_active=1 AND a.approval_status='active' LIMIT 1`).bind(await hash(t)).first<HomeIdentity>()};
const getBranding=async(env:Env)=>{const keys=['brand_main_logo','brand_login_logo','brand_favicon','footer_text','theme_accent','theme_icon'],q=await env.DB.prepare(`SELECT key,value FROM settings WHERE key IN (${keys.map(()=>'?').join(',')})`).bind(...keys).all<{key:string;value:string}>();return Object.fromEntries((q.results??[]).map(x=>[x.key,x.value])) as Branding};
const assetUrl=(key?:string)=>key?`/assets/${encodeURIComponent(key)}`:"";
const fallbackBrand=(tag:string|null|undefined,name:string)=>esc((tag?.trim()||name.trim()||"Alliance").slice(0,5));
const memberHome=async(request:Request,env:Env,who:HomeIdentity)=>{const alliance=await env.DB.prepare("SELECT name,tag,server_number FROM alliance WHERE id=1 LIMIT 1").first<{name:string;tag:string|null;server_number:number|null}>().catch(()=>null),s=await getBranding(env).catch(()=>({} as Branding)),name=alliance?.name||"Alliance",tag=alliance?.tag?`[${esc(alliance.tag)}] · `:"",server=alliance?.server_number?`Server #${alliance.server_number}`:"Server not set",rankTitle=who.show_rank_names!=="0"&&who.rank_name&&who.rank_name!==`R${who.rank}`?` · ${esc(who.rank_name)}`:"",accent=/^#[0-9a-fA-F]{6}$/.test(s.theme_accent||"")?s.theme_accent!:"#5865f2",logo=s.brand_main_logo?`<img src="${assetUrl(s.brand_main_logo)}" alt="${esc(name)}">`:`<span>${fallbackBrand(alliance?.tag,name)}</span>`,leadership=who.is_owner?`<a class="leadership" href="/leadership"><strong>Leadership Console</strong><span>Management, operations and alliance settings →</span></a>`:"";return new Response(`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(name)} · Home</title>${s.brand_favicon?`<link rel="icon" href="${assetUrl(s.brand_favicon)}">`:""}<style>:root{color-scheme:dark;font-family:Inter,system-ui,sans-serif;--accent:${accent}}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(circle at top,#17243d 0,#0c1220 45%,#090e18 100%);color:#eef3ff;padding:24px}main{width:min(94vw,1120px);margin:28px auto;background:#151d2e;border:1px solid #2b3850;border-radius:20px;padding:30px;box-shadow:0 24px 70px rgba(0,0,0,.35)}header{display:flex;align-items:center;gap:14px;padding-bottom:22px;border-bottom:1px solid #2b3850}.mark{width:58px;height:58px;flex:0 0 58px;border-radius:15px;background:var(--accent);display:flex;align-items:center;justify-content:center;overflow:hidden;font-weight:900}.mark img{width:100%;height:100%;object-fit:contain}.alliance strong,.alliance span{display:block}.alliance strong{font-size:1.15rem}.alliance span,.user span{color:#90a0bb;font-size:.82rem;margin-top:3px}.user{margin-left:auto;text-align:right}.user strong,.user span{display:block}.hero{padding:34px 2px 18px}.eyebrow{color:#8292ae;text-transform:uppercase;letter-spacing:.08em;font-size:.76rem;font-weight:900}.hero h1{font-size:2rem;margin:7px 0 8px}.hero p{margin:0;color:#aebddd;line-height:1.55}.placeholder{margin-top:22px;padding:26px;border:1px dashed #405274;border-radius:16px;background:#0e1626}.placeholder h2{margin:0 0 8px;font-size:1.08rem}.placeholder p{margin:0;color:#90a0bb;line-height:1.55}.leadership{display:flex;justify-content:space-between;gap:18px;align-items:center;margin-top:18px;padding:18px;border:1px solid #34445f;border-radius:14px;background:#111b2e;color:#eef3ff;text-decoration:none}.leadership span{color:#90a0bb;font-size:.86rem}.footer{display:flex;justify-content:space-between;gap:14px;margin-top:28px;padding-top:18px;border-top:1px solid #2b3850;color:#8292ae;font-size:.8rem}.footer a{color:#aebddd;text-decoration:none}@media(max-width:650px){body{padding:12px}main{padding:20px;margin:8px auto}header{align-items:flex-start}.mark{width:50px;height:50px;flex-basis:50px}.user{font-size:.86rem}.hero{padding-top:26px}.hero h1{font-size:1.55rem}.leadership,.footer{display:block}.leadership span{display:block;margin-top:5px}.footer a{display:inline-block;margin-top:8px}}</style></head><body><main><header><div class="mark">${logo}</div><div class="alliance"><strong>${esc(name)}</strong><span>${tag}${server}</span></div><div class="user"><strong>${esc(who.display_name)}</strong><span>R${who.rank}${rankTitle}</span></div></header><section class="hero"><div class="eyebrow">Alliance Home</div><h1>Welcome to ${esc(name)}</h1><p>Your alliance information, roster and member tools will live here.</p></section><section class="placeholder"><h2>Alliance overview</h2><p>The member home is ready for the alliance roster and information as we build out the platform.</p></section>${leadership}<div class="footer"><span>${s.footer_text?.trim()?esc(s.footer_text.trim()):`${tag}${server}`}</span><a href="/auth/logout">Sign out</a></div></main></body></html>`,{headers:{"content-type":"text/html; charset=utf-8","cache-control":"no-store","x-frame-options":"DENY","referrer-policy":"no-referrer"}})};
const polishPending=async(res:Response,env:Env)=>{if(!res.headers.get("content-type")?.includes("text/html"))return res;let html=await res.text();const alliance=await env.DB.prepare("SELECT name,tag,server_number FROM alliance WHERE id=1 LIMIT 1").first<{name:string;tag:string|null;server_number:number|null}>().catch(()=>null),s=await getBranding(env).catch(()=>({} as Branding)),name=alliance?.name||"Alliance",identity=`${alliance?.tag?`[${esc(alliance.tag)}] · `:""}${alliance?.server_number?`Server #${alliance.server_number}`:""}`,logo=s.brand_login_logo||s.brand_main_logo,accent=/^#[0-9a-fA-F]{6}$/.test(s.theme_accent||"")?s.theme_accent!:"#5865f2",icon=/^#[0-9a-fA-F]{6}$/.test(s.theme_icon||"")?s.theme_icon!:"#aebddd",logoHtml=logo?`<img src="${assetUrl(logo)}" alt="${esc(name)}">`:`<span class="brandfallback">${fallbackBrand(alliance?.tag,name)}</span>`;html=html.replace('<div class="duck">🦆</div>',`<div class="pendingbrand">${logoHtml}</div>`);html=html.replace('<a href="/auth/logout">Sign out</a>',`<a class="pendingSignout" href="/auth/logout">Sign out</a><div class="pendingfooter"><strong>${esc(name)}</strong><span>${s.footer_text?.trim()?esc(s.footer_text.trim()):identity}</span></div>`);if(s.brand_favicon)html=html.replace('</title>',`</title><link rel="icon" href="${assetUrl(s.brand_favicon)}">`);html=html.replace('</head>',`<style>:root{--ap-accent:${accent};--ap-icon:${icon}}.duck{display:none}.pendingbrand{width:92px;height:92px;margin:0 auto 18px;display:flex;align-items:center;justify-content:center;border-radius:20px;overflow:hidden}.pendingbrand img{display:block;width:100%;height:100%;object-fit:contain}.brandfallback{width:100%;height:100%;display:flex;align-items:center;justify-content:center;border-radius:20px;background:var(--ap-accent);font-weight:900}.pendingSignout{display:inline-block;margin-top:4px;padding:12px 22px;border-radius:11px;background:var(--ap-accent);color:#fff!important;font-weight:800;text-decoration:none}.pendingfooter{display:flex;justify-content:space-between;gap:14px;margin-top:28px;padding-top:17px;border-top:1px solid #2b3850;color:#8292ae;font-size:.78rem;text-align:left}.pendingfooter strong{color:var(--ap-icon)}.pendingfooter span{text-align:right}@media(max-width:650px){.pendingfooter{display:block;text-align:center}.pendingfooter span{display:block;text-align:center;margin-top:5px}}</style></head>`);return withHtml(res,html)};
export default {async fetch(request:Request,env:Env):Promise<Response>{const url=new URL(request.url);if(url.pathname==="/security-access")return Response.redirect(new URL("/security/access",request.url),302);if(request.method==="GET"&&url.pathname==="/"){const who=await homeIdentity(request,env).catch(()=>null);if(who)return memberHome(request,env,who)}let downstream=request;if(request.method==="GET"&&url.pathname==="/leadership"){const who=await homeIdentity(request,env).catch(()=>null);if(!who||!who.is_owner)return Response.redirect(new URL("/",request.url),302);const rewritten=new URL(request.url);rewritten.pathname="/";downstream=new Request(rewritten.toString(),request)}const result=await(app as any).fetch(downstream,env);if(!(result instanceof Response)){if(typeof result==="string")return new Response(result,{headers:{"content-type":"text/html; charset=utf-8"}});throw new TypeError("Worker handler did not return a Response")};if(url.pathname==="/leadership"&&result.headers.get("content-type")?.includes("text/html")){let html=await result.text();html=html.replace(/<title>.*?<\/title>/i,`<title>Leadership Console</title>`).replaceAll('href="/">Leadership Console','href="/leadership">Leadership Console').replaceAll('href="/">← Leadership Console','href="/leadership">← Leadership Console');return withHtml(result,html)}if(["/pending","/access-declined","/access-blocked"].includes(url.pathname))return polishPending(result,env);return result}} satisfies ExportedHandler<Env>;
